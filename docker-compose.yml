services:
  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: tms-rabbitmq
    hostname: tms-rabbitmq
    profiles: ["app"]
    restart: "no"
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 12
    networks:
      tms-network:
        aliases:
          - rabbitmq
          - tms-rabbitmq

  sonarqube-db:
    image: postgres:16
    container_name: tms-sonarqube-db
    profiles: ["quality"]
    restart: "no"
    environment:
      POSTGRES_DB: sonarqube
      POSTGRES_USER: sonarqube
      POSTGRES_PASSWORD: sonarqube
    volumes:
      - sonarqube_db_data:/var/lib/postgresql/data
    networks:
      - tms-network

  sonarqube:
    image: sonarqube:community
    container_name: tms-sonarqube
    profiles: ["quality"]
    restart: "no"
    depends_on:
      sonarqube-db:
        condition: service_started
    environment:
      SONAR_JDBC_URL: jdbc:postgresql://sonarqube-db:5432/sonarqube
      SONAR_JDBC_USERNAME: sonarqube
      SONAR_JDBC_PASSWORD: sonarqube
      SONAR_WEB_JAVAOPTS: -Xms256m -Xmx256m
      SONAR_CE_JAVAOPTS: -Xms256m -Xmx256m
      SONAR_SEARCH_JAVAOPTS: -Xms256m -Xmx256m -XX:MaxDirectMemorySize=128m -Dnode.store.allow_mmap=false
    ports:
      - "9000:9000"
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_logs:/opt/sonarqube/logs
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    networks:
      - tms-network

  loki:
    image: grafana/loki:3.1.1
    container_name: tms-loki
    profiles: ["app"]
    restart: "no"
    command: -config.file=/etc/loki/config.yaml
    ports:
      - "3100:3100"
    volumes:
      - ./observability/loki/loki-config.yaml:/etc/loki/config.yaml:ro
      - loki_data:/loki
    networks:
      - tms-network

  promtail:
    image: grafana/promtail:3.1.1
    container_name: tms-promtail
    profiles: ["app"]
    restart: "no"
    command: -config.file=/etc/promtail/config.yaml
    depends_on:
      loki:
        condition: service_started
    volumes:
      - ./observability/promtail/promtail-config.yaml:/etc/promtail/config.yaml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - tms-network

  grafana:
    image: grafana/grafana:11.1.4
    container_name: tms-grafana
    profiles: ["app"]
    restart: "no"
    depends_on:
      loki:
        condition: service_started
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./observability/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./observability/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
    networks:
      - tms-network

  zipkin:
    image: openzipkin/zipkin:3
    container_name: tms-zipkin
    profiles: ["app"]
    restart: "no"
    ports:
      - "9411:9411"
    networks:
      - tms-network

  eureka-server:
    image: arpityadav005/tms-eureka-server:latest
    platform: linux/amd64
    container_name: tms-eureka-server
    profiles: ["app"]
    restart: unless-stopped
    ports:
      - "8761:8761"
    environment:
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx256m -XX:+UseSerialGC
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8761/actuator/health | grep -q UP"]
      interval: 15s
      timeout: 10s
      retries: 30
      start_period: 90s
    networks:
      - tms-network

  config-server:
    image: arpityadav005/tms-config-server:latest
    platform: linux/amd64
    container_name: tms-config-server
    profiles: ["app"]
    restart: unless-stopped
    depends_on:
      eureka-server:
        condition: service_healthy
    environment:
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx256m -XX:+UseSerialGC
      CONFIG_REPO_URI: ${CONFIG_REPO_URI}
      CONFIG_REPO_USERNAME: ${CONFIG_REPO_USERNAME:-}
      CONFIG_REPO_TOKEN: ${CONFIG_REPO_TOKEN:-}
      EUREKA_URL: http://eureka-server:8761/eureka/
    ports:
      - "7777:7777"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --user=config-admin --password=config@123 -O- http://localhost:7777/actuator/health | grep -q UP"]
      interval: 15s
      timeout: 10s
      retries: 30
      start_period: 90s
    networks:
      - tms-network

  auth-service:
    image: arpityadav005/tms-auth-service:latest
    platform: linux/amd64
    container_name: tms-auth-service
    profiles: ["app"]
    restart: unless-stopped
    depends_on:
      zipkin:
        condition: service_started
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    environment:
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx256m -XX:+UseSerialGC -Dspring.config.import=optional:configserver:http://config-admin:config%40123@config-server:7777 -Deureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
      SERVER_PORT: 8081
      CONFIG_SERVER_IMPORT: optional:configserver:http://config-admin:config%40123@config-server:7777
      EUREKA_DEFAULT_ZONE: http://eureka-server:8761/eureka/
      SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:${MYSQL_PORT:-3306}/${AUTH_DB_NAME:-auth_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-tms}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-tms}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-86400000}
      MANAGEMENT_ZIPKIN_TRACING_ENDPOINT: http://zipkin:9411/api/v2/spans
    ports:
      - "8081:8081"
    volumes:
      - ./auth-service/src/main/resources/application.yaml:/app/config/application.yaml:ro
    networks:
      - tms-network

  timesheet-service:
    image: arpityadav005/tms-timesheet-service:latest
    platform: linux/amd64
    container_name: tms-timesheet-service
    profiles: ["app"]
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
      zipkin:
        condition: service_started
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    environment:
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx256m -XX:+UseSerialGC -Dspring.config.import=optional:configserver:http://config-admin:config%40123@config-server:7777 -Deureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
      SERVER_PORT: 8082
      CONFIG_SERVER_IMPORT: optional:configserver:http://config-admin:config%40123@config-server:7777
      EUREKA_DEFAULT_ZONE: http://eureka-server:8761/eureka/
      SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:${MYSQL_PORT:-3306}/${TIMESHEET_DB_NAME:-timesheet_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-tms}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-tms}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-86400000}
      SPRING_RABBITMQ_HOST: tms-rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_RABBITMQ_USERNAME: guest
      SPRING_RABBITMQ_PASSWORD: guest
      MANAGEMENT_ZIPKIN_TRACING_ENDPOINT: http://zipkin:9411/api/v2/spans
    ports:
      - "8082:8082"
    volumes:
      - ./timesheet-service/src/main/resources/application.yaml:/app/config/application.yaml:ro
    networks:
      - tms-network

  leave-service:
    image: arpityadav005/tms-leave-service:latest
    platform: linux/amd64
    container_name: tms-leave-service
    profiles: ["app"]
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
      zipkin:
        condition: service_started
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    environment:
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx256m -XX:+UseSerialGC -Dspring.config.import=optional:configserver:http://config-admin:config%40123@config-server:7777 -Deureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
      SERVER_PORT: 8083
      CONFIG_SERVER_IMPORT: optional:configserver:http://config-admin:config%40123@config-server:7777
      EUREKA_DEFAULT_ZONE: http://eureka-server:8761/eureka/
      SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:${MYSQL_PORT:-3306}/${LEAVE_DB_NAME:-leave_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-tms}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-tms}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-86400000}
      SPRING_RABBITMQ_HOST: tms-rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_RABBITMQ_USERNAME: guest
      SPRING_RABBITMQ_PASSWORD: guest
      MANAGEMENT_ZIPKIN_TRACING_ENDPOINT: http://zipkin:9411/api/v2/spans
    ports:
      - "8083:8083"
    volumes:
      - ./leave-service/src/main/resources/application.yaml:/app/config/application.yaml:ro
    networks:
      - tms-network

  admin-service:
    image: arpityadav005/tms-admin-service:latest
    platform: linux/amd64
    container_name: tms-admin-service
    profiles: ["app"]
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
      zipkin:
        condition: service_started
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    environment:
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx256m -XX:+UseSerialGC -Dspring.config.import=optional:configserver:http://config-admin:config%40123@config-server:7777 -Deureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
      SERVER_PORT: 8084
      CONFIG_SERVER_IMPORT: optional:configserver:http://config-admin:config%40123@config-server:7777
      EUREKA_DEFAULT_ZONE: http://eureka-server:8761/eureka/
      SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:${MYSQL_PORT:-3306}/${ADMIN_DB_NAME:-admin_db}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-tms}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-tms}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-86400000}
      SPRING_RABBITMQ_HOST: tms-rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_RABBITMQ_USERNAME: guest
      SPRING_RABBITMQ_PASSWORD: guest
      MANAGEMENT_ZIPKIN_TRACING_ENDPOINT: http://zipkin:9411/api/v2/spans
    ports:
      - "8084:8084"
    volumes:
      - ./admin-service/src/main/resources/application.yaml:/app/config/application.yaml:ro
    networks:
      - tms-network

  api-gateway:
    image: arpityadav005/tms-api-gateway:latest
    platform: linux/amd64
    container_name: tms-api-gateway
    profiles: ["app"]
    restart: unless-stopped
    depends_on:
      zipkin:
        condition: service_started
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
      auth-service:
        condition: service_started
      timesheet-service:
        condition: service_started
      leave-service:
        condition: service_started
      admin-service:
        condition: service_started
    environment:
      JAVA_TOOL_OPTIONS: -Xms128m -Xmx256m -XX:+UseSerialGC -Dspring.config.import=optional:configserver:http://config-admin:config%40123@config-server:7777 -Deureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
      SERVER_PORT: 8080
      CONFIG_SERVER_IMPORT: optional:configserver:http://config-admin:config%40123@config-server:7777
      EUREKA_DEFAULT_ZONE: http://eureka-server:8761/eureka/
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-86400000}
      MANAGEMENT_ZIPKIN_TRACING_ENDPOINT: http://zipkin:9411/api/v2/spans
    ports:
      - "8080:8080"
    volumes:
      - ./api-gateway/src/main/resources/application.yaml:/app/config/application.yaml:ro
    networks:
      - tms-network

networks:
  tms-network:
    driver: bridge

volumes:
  sonarqube_data:
  sonarqube_extensions:
  sonarqube_logs:
  sonarqube_db_data:
  loki_data:
  grafana_data:
